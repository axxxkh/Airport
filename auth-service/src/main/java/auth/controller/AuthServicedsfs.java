package auth.controller;

import lombok.AllArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@AllArgsConstructor
public class AuthServicedsfs {
//    private final RestTemplate restTemplate;
//    private JwtTokenUtil jwtUtil;
//    private PasswordEncoder passwordEncoder;

//    @Bean
//    public PasswordEncoder passwordEncoder() {
//        return new BCryptPasswordEncoder();
//    }

//    private boolean isEmailBusy(String email) {
//// to implement
//        AuthResponse authResponse = restTemplate.getForObject("localhost:8080/users", AuthResponse.class, email);
//
//      return true;
//    }
//
//    public AuthResponse login(LoginRequest request) {
//        request.setPassword(passwordEncoder.encode(request.getPassword()));
//      return   restTemplate.postForObject("localhost:8080/users/", request, AuthResponse.class);
//    }
//
//    public AuthResponse register(RegisterRequest request) {
//        return   restTemplate.postForObject("localhost:8080/users/register", request, AuthResponse.class);
//    }
//

}
